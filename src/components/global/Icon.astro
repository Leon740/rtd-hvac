---
interface IIconProps {
  icon: 'arrow' | 'bars' | 'calendar';
  className?: string;
}

const { icon, className = '' } = Astro.props as IIconProps;
---

<i class={`icon-${icon} ${className}`}></i>
